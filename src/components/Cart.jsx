import React, { useState } from "react";

const Cart = ({ cart, setCart }) => {
    const [price, setPrice] = useState(0);
  return (
    <div>
      <h1>ffaersydghsxd</h1>
    </div>
  );
};

export default Cart;
